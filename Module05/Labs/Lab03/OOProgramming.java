class OOProgramming {

    public static void main(String[] args) {

        // Define an array of three Person references
        // Initialize it with one Person and two Employees, the order does not matter
        // Write a loop (any kind will do) to visit each element of the array and call
        // the display function.
        // Compile and test. Note that the compiler automatically chooses the
        // correct version of display depending on if the array element refers to
        // a Person or an Employee

        // Write some code here ....


        // Once you have this working add an override of toString into Person
        // Have it return a human readable string including the name and age.
        // toString should NOT print anything.

        // Add an override of toString into class Employee
        // Have it return a human readable string including
        // the name, the age, the role and the salary. Get the
        // name and age part by calling super.toString().


        // Back in this file, use a loop to visit each item in the array; call
        // toString and print the result
        // Write your loop here...

    }
}